[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "bugzilla-cli"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "httpx>=0.28",
    "python-dotenv>=1.1",
]

[tool.setuptools]
py-modules = ["client", "render"]
